syntax = "proto3";

package rpdb;

message WALEntry {
  enum OpType{
    BEGIN = 0;
    SET = 1;
    UNSET = 2;
    COMMIT = 3;
    ROLLBACK = 4;
  }

  int64 timestamp = 1;
  OpType op_type = 2;
  string key = 3;
  string value = 4;
}

message WAL {
  repeated WALEntry entries = 1;
}
